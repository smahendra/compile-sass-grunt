#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../svg-cleaner/bin/svg-cleaner" "$@"
  ret=$?
else 
  node  "$basedir/../svg-cleaner/bin/svg-cleaner" "$@"
  ret=$?
fi
exit $ret
